#include <iostream>
using namespace std;

int main() {
    int t,a,b,c,d ;
    cin>>t;
    while(t--)
    {
        cin>> a>>b>>c>>d;
        if (a==1 || b==1 || c==1 || d==1) cout << "OUT\n";
        else 
        cout <<"IN\n";
    }
	
	return 0;
}
